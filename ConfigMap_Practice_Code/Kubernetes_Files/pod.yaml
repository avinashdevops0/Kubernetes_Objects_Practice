apiVersion: v1
kind: Pod
metadata:
  name: welcome-pod
  labels:
    app: welcome-app
spec:
  containers:
    - name: welcome-container
      image: nginx:latest
      ports:
        - containerPort: 80
      envFrom:
        - configMapRef:
            name: welcome-configMap